# This project uses Node.js and npm for dependency management.
# For Python environments, this file is for documentation only.
# Main dependencies are listed in package.json, but for reference:

# React and Vite
react
react-dom
vite

# EmailJS for browser
@emailjs/browser

# Tailwind CSS (if used)
tailwindcss
postcss

# Framer Motion (if used for animations)
framer-motion

# Other utilities (if present in your package.json)

# To install dependencies, use:
# npm install 